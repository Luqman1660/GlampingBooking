import 'package:flutter/material.dart';

class AdminBookingList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Admin Booking List'),
    );
  }
}